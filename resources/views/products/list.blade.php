<x-layouts.app><div class="d-flex justify-content-between mb-3"><h2>Product List</h2><a href="{{ route('products.create') }}" class="btn btn-primary">Add new product</a></div>@foreach($products as $product)<x-card><h5>{{ $product['name'] }}</h5><p>{{ $product['description'] }}</p><p><strong>Rp {{ number_format($product['price'],0,',','.') }}</strong></p><a href="{{ route('products.show',$product['id']) }}" class="btn btn-sm btn-info">Detail</a><a href="{{ route('products.edit',$product['id']) }}" class="btn btn-sm btn-warning">Edit</a></x-card>@endforeach</x-layouts.app>